//! `samplecheck` -- run the logits filters against the committed conformance corpus.
//!
//! ```text
//! samplecheck <sample_corpus.json> [--verbose] [--max N] [--frequency N]
//! ```
//!
//! The corpus is generated by `tools/make_sample_corpus.py` and stores, for each case, the
//! input logits and **the reference's complete filtered vector**. Comparing the whole
//! vector rather than the kept set is the point: a wrong temperature or a wrong penalty
//! magnitude changes values without changing which tokens survive, and a support-only check
//! would call that a pass.
//!
//! Three things are checked:
//!
//! * **the filtered logits**, bit-exactly. `temperature` is the one exception -- it divides
//!   by an arbitrary value, which is not exact in either language -- and those cases are
//!   allowed one ulp.
//! * **the stage order**, against the order the corpus records. The corpus is built in the
//!   reference's own order, so a pipeline that disagrees would still match on every
//!   single-filter case and only diverge on the combinations.
//! * **the draw**, by frequency rather than by token (`--frequency`). A sampler cannot be
//!   compared token-for-token with the reference, because the generators differ; what can
//!   be compared is the distribution it produces.

use std::process::ExitCode;

use gdn::pyjson::{self, Value};
use gdn::sample::{self, SamplerConfig, FILTERED};

/// The order this tree's pipeline runs in. Must equal the corpus's `stage_order`; if the
/// reference ever reorders, this fails loudly rather than quietly comparing unequal things.
const OUR_ORDER: &[&str] = &[
    "repetition_penalty",
    "presence_penalty",
    "frequency_penalty",
    "no_repeat_ngram",
    "temperature",
    "top_k",
    "top_p",
    "min_p",
    "typical_p",
];

fn main() -> ExitCode {
    let args: Vec<String> = std::env::args().collect();
    let flag = |n: &str| args.iter().any(|a| a == n);
    let val = |n: &str| -> Option<String> {
        args.iter().position(|a| a == n).and_then(|i| args.get(i + 1)).cloned()
    };
    let positional: Vec<&String> = args.iter().skip(1).filter(|a| !a.starts_with("--")).collect();
    if positional.is_empty() {
        eprintln!("usage: samplecheck <sample_corpus.json> [--verbose] [--max N] [--frequency N]");
        return ExitCode::from(2);
    }
    let verbose = flag("--verbose");
    let max: Option<usize> = val("--max").and_then(|s| s.parse().ok());

    let raw = match std::fs::read(positional[0]) {
        Ok(v) => v,
        Err(e) => {
            eprintln!("error: {}: {e}", positional[0]);
            return ExitCode::FAILURE;
        }
    };
    let doc = match pyjson::parse(&String::from_utf8_lossy(&raw)) {
        Ok(v) => v,
        Err(e) => {
            eprintln!("error: {}: {e}", positional[0]);
            return ExitCode::FAILURE;
        }
    };
    let cases = match doc.get("cases").and_then(|c| c.as_array()) {
        Some(c) => c,
        None => {
            eprintln!("error: no `cases`");
            return ExitCode::FAILURE;
        }
    };
    let vocab = doc.get("vocab").and_then(|v| v.as_usize()).unwrap_or(0);
    let reference = doc.get("reference").and_then(|v| v.as_str()).unwrap_or("?");

    println!("== sampling");
    println!("   reference   {reference}");
    println!("   cases       {}", cases.len());
    println!("   vocab       {vocab}");
    println!();

    // The order first, because every case below assumes it.
    let mut failed = false;
    let want_order: Vec<String> = doc
        .get("stage_order")
        .and_then(|v| v.as_array())
        .map(|a| a.iter().filter_map(|v| v.as_str().map(str::to_string)).collect())
        .unwrap_or_default();
    if want_order.iter().map(String::as_str).collect::<Vec<_>>() != OUR_ORDER {
        println!("  !! the pipeline order differs from the corpus's");
        println!("     corpus {want_order:?}");
        println!("     ours   {OUR_ORDER:?}");
        failed = true;
    } else {
        println!("   order is the reference's: {}", OUR_ORDER.join(" -> "));
        // `presence_penalty` and `frequency_penalty` are absent from the reference, so the
        // corpus cannot confirm where they belong. Said here rather than glossed over.
        println!(
            "   note: presence_penalty and frequency_penalty are not in this reference \
             version, so\n         their placement is the documented order and is not \
             reference-confirmed"
        );
    }
    println!();

    let (mut ok, mut bad) = (0usize, 0usize);
    let mut n_exact = 0usize;
    let mut n_ulp = 0usize;
    let mut n_ties = 0usize;
    let mut n_tie_index_mismatch = 0usize;
    // Cases where the reference's `top_p` truncation lands within a couple of ulp of the
    // threshold, so the count is decided by scan precision rather than by the rule.
    let mut n_boundary: Vec<String> = Vec::new();
    let mut first: Option<String> = None;
    // Failures by the last stage that ran, so a failure names the filter.
    let mut by_stage: Vec<(String, usize)> = Vec::new();

    for (i, case) in cases.iter().enumerate() {
        if max.is_some_and(|m| i >= m) {
            break;
        }
        let name = case.get("name").and_then(|n| n.as_str()).unwrap_or("?");
        let settings = case.get("settings").cloned().unwrap_or(Value::Null);
        let (Some(expect), Some(logits_json)) = (
            case.get("expect").and_then(|v| v.as_array()),
            case.get("logits").and_then(|v| v.as_array()),
        ) else {
            bad += 1;
            continue;
        };

        // `null` is -inf, which JSON cannot spell.
        let mut logits: Vec<f32> = Vec::with_capacity(logits_json.len());
        for v in logits_json {
            match decode_lit(v) {
                Some(f) => logits.push(f),
                None => {
                    eprintln!("error: {name}: logit {v:?} is not a number or null");
                    return ExitCode::FAILURE;
                }
            }
        }
        let history: Vec<u32> = case
            .get("history")
            .and_then(|v| v.as_array())
            .map(|a| a.iter().filter_map(|v| v.as_usize()).map(|v| v as u32).collect())
            .unwrap_or_default();

        let cfg = config_from(&settings);
        let sampler = match sample::Sampler::new(cfg) {
            Ok(s) => s,
            Err(e) => {
                let msg = format!("case {i} {name}: {e}");
                println!("  !! {msg}");
                bad += 1;
                failed = true;
                first.get_or_insert(msg);
                continue;
            }
        };
        // How many cases have duplicate input values, where the reference's sort order --
        // and therefore which of several tied tokens is masked -- is not reproducible.
        if has_duplicate_values(&logits) {
            n_ties += 1;
        }
        let mut got = logits.clone();
        sampler.apply_filters(&mut got, &history);

        // Per-index agreement is the strong check. Where it fails on a tie-ambiguous case
        // it is counted separately instead of being called a pass or a failure, so the
        // amount of behaviour the corpus cannot pin down is visible rather than assumed to
        // be zero.
        if has_duplicate_values(&logits) && verdict_of(&got, expect) {
            n_tie_index_mismatch += 1;
        }

        // `temperature` divides by an arbitrary value, so it cannot be bit-exact in both
        // languages; it gets one ulp. Everything else must be identical.
        let has_temperature = settings
            .get("temperature")
            .and_then(|v| v.as_f64())
            .is_some_and(|t| t != 1.0);

        match compare(&got, expect, &logits, has_temperature) {
            Verdict::Exact => {
                n_exact += 1;
                ok += 1;
            }
            Verdict::WithinUlp => {
                n_ulp += 1;
                ok += 1;
            }
            Verdict::Differ { at, got: g, want: w } => {
                // Before calling this a failure: does the reference's kept count sit
                // within a couple of ulp of the `top_p` threshold? If so the *rule* does not
                // decide it -- a cumulative mass that is one ulp under the threshold is
                // included, one ulp over is not -- and the reference's internal scan
                // precision is what answers. That is worth reporting by name rather than
                // either failing on it or hiding it behind a blanket tolerance.
                if let Some(why) = boundary_case(&settings, &logits, expect) {
                    n_boundary.push(format!("case {i} {name}: {why}"));
                    continue;
                }
                bad += 1;
                failed = true;
                let msg = format!(
                    "case {i} {name}: filtered logits differ at {at}: got {} want {w}",
                    spell(g)
                );
                println!("  !! {msg}");
                if verbose {
                    let kg = got.iter().filter(|v| **v != FILTERED).count();
                    let kw = expect.iter().filter(|v| !matches!(v, Value::Null)).count();
                    println!("     kept {kg} vs {kw} of {}", got.len());
                }
                first.get_or_insert(msg);
                let stage = last_stage(&settings);
                match by_stage.iter_mut().find(|(s, _)| *s == stage) {
                    Some((_, n)) => *n += 1,
                    None => by_stage.push((stage, 1)),
                }
            }
        }
    }

    println!();
    if let Some(f) = &first {
        println!("   first failure: {f}");
    }
    if !by_stage.is_empty() {
        println!("   failures by the last stage that ran:");
        by_stage.sort_by_key(|(_, n)| std::cmp::Reverse(*n));
        for (s, n) in &by_stage {
            println!("     {s:24} {n}");
        }
    }
    println!("   logits {ok} ok  {bad} bad   ({n_exact} bit-exact, {n_ulp} within one ulp)");
    if !n_boundary.is_empty() {
        println!();
        println!(
            "   {} case(s) where the reference's `top_p` boundary lands within 2 ulp of the\n   \
             threshold, so the kept count is decided by its internal scan precision and not\n   \
             by the rule. Reported, not failed:",
            n_boundary.len()
        );
        for w in &n_boundary {
            println!("     {w}");
        }
    }
    println!(
        "   {n_ties} of {} cases have duplicate input logits; there the reference's radix sort\n   \
         order decides which of several tied tokens is masked, so those are compared as a\n   \
         multiset rather than index for index (see the comment on `compare`)",
        cases.len()
    );
    println!(
        "   of those, {n_tie_index_mismatch} differ at the index level -- the reference's \
         tie-break,\n   which is not reproducible; every duplicate-free case is compared \
         index for index"
    );

    let freq = val("--frequency").and_then(|s| s.parse::<usize>().ok()).unwrap_or(0);
    if freq > 0 {
        println!();
        println!("   distribution check: {freq} draws per case");
        if !frequency_check(freq, verbose) {
            failed = true;
        }
    }

    println!();
    println!("   RESULT: {}", if failed { "FAIL" } else { "PASS" });
    if failed {
        ExitCode::FAILURE
    } else {
        ExitCode::SUCCESS
    }
}

enum Verdict {
    Exact,
    WithinUlp,
    Differ { at: usize, got: f32, want: String },
}

/// `null` is `-inf`, a bare number is a logit, and the string `"nan"` is NaN. Nothing else
/// is accepted: a corpus that quietly turned a typo into `-inf` would compare equal to a
/// fully-masked pipeline and pass.
fn decode_lit(v: &Value) -> Option<f32> {
    match v {
        Value::Null => Some(FILTERED),
        Value::Float(f) => Some(*f as f32),
        Value::Int(n) => Some(*n as f32),
        Value::Str(s) if s == "nan" => Some(f32::NAN),
        _ => None,
    }
}

fn spell(v: f32) -> String {
    if v == FILTERED {
        "-inf".to_string()
    } else if v.is_nan() {
        "nan".to_string()
    } else {
        format!("{v}")
    }
}

/// Compare the filtered logits, **as a multiset when the input has ties**.
///
/// This is subtler than it looks, and the subtlety is not ours to choose.
///
/// `torch.sort` is not stable. Its radix sort hands an all-equal vector back in an
/// effectively arbitrary permutation, and that permutation *is* observable: `top_p`'s
/// ascending cumulative scan masks the tokens it sees first, so when several tokens tie at
/// the truncation boundary, which of them gets masked depends on the sort order. There is
/// no implementation of that order to copy -- it is a property of how torch hands 32
/// elements at a time to a bitonic network -- so requiring the same *index* to be masked
/// would be requiring the impossible.
///
/// What is not arbitrary is the multiset. Sorting both sides makes the comparison exactly
/// as strong as the per-index one whenever the input has **no duplicate values**, because
/// then the mask set determines the values one for one; and it is deliberately blind to
/// the tie-break when duplicates exist, which is the only honest thing to be.
///
/// So: the sorted vectors must match, *and* when the input is duplicate-free the per-index
/// vectors must match too. The second clause is what stops "sorted" from hiding a real
/// error, since a duplicate-free case that masks the wrong index fails it.
fn compare(got: &[f32], expect: &[Value], input: &[f32], allow_ulp: bool) -> Verdict {
    if got.len() != expect.len() {
        return Verdict::Differ {
            at: got.len().min(expect.len()),
            got: f32::NAN,
            want: format!("{} values", expect.len()),
        };
    }
    let want: Vec<f32> = {
        let mut v = Vec::with_capacity(expect.len());
        for (k, w) in expect.iter().enumerate() {
            match decode_lit(w) {
                Some(f) => v.push(f),
                None => return Verdict::Differ { at: k, got: got[k], want: format!("{w:?}") },
            }
        }
        v
    };

    let has_ties = has_duplicate_values(input);

    // `total_cmp` gives -inf a place below every finite value and NaN one above, which is
    // the order the reference's radix sort produces too. A plain `partial_cmp` would need
    // a tie-break and would reintroduce the very ambiguity this avoids.
    let mut a: Vec<f32> = got.to_vec();
    let mut b: Vec<f32> = want.clone();
    a.sort_by(f32::total_cmp);
    b.sort_by(f32::total_cmp);

    let mut within = true;
    for k in 0..a.len() {
        let (x, y) = (a[k], b[k]);
        if x == y {
            continue;
        }
        if allow_ulp && x.is_finite() && y.is_finite() && (x - y).abs() <= y.abs() * f32::EPSILON * 2.0
        {
            within = false;
            continue;
        }
        return Verdict::Differ { at: k, got: x, want: spell(y) };
    }

    // Duplicate-free input: the tie-break cannot have mattered, so the per-index result
    // must match exactly.
    if !has_ties {
        for k in 0..a.len() {
            if got[k] != want[k] {
                if allow_ulp
                    && got[k].is_finite()
                    && want[k].is_finite()
                    && (got[k] - want[k]).abs() <= want[k].abs() * f32::EPSILON * 2.0
                {
                    within = false;
                    continue;
                }
                return Verdict::Differ { at: k, got: got[k], want: spell(want[k]) };
            }
        }
    }
    if within {
        Verdict::Exact
    } else {
        Verdict::WithinUlp
    }
}

/// Whether any value appears twice, which is what makes the reference's sort order
/// observable at all. `-inf` counts: several masked tokens are as much a tie as several
/// equal logits.
fn has_duplicate_values(v: &[f32]) -> bool {
    let mut b: Vec<u32> = v.iter().map(|x| x.to_bits()).collect();
    b.sort_unstable();
    b.windows(2).any(|w| w[0] == w[1])
}

/// True when the per-index vectors differ, i.e. the tie-break is what is being excused.
fn verdict_of(got: &[f32], expect: &[Value]) -> bool {
    expect.iter().enumerate().any(|(k, w)| match decode_lit(w) {
        Some(x) => match got.get(k) {
            Some(&g) => {
                if x == FILTERED || g == FILTERED {
                    g != x
                } else if x.is_nan() {
                    !g.is_nan()
                } else {
                    g != x
                }
            }
            None => true,
        },
        None => true,
    })
}

/// Whether the reference's `top_p` truncation is undecidable, with the reason.
///
/// The rule is "mask while the ascending cumulative mass is `<= 1 - top_p`". If that
/// boundary falls *exactly* between two tokens, the count is decided by whatever rounding
/// each implementation's scan produces, and the reference's is not reproducible. This is
/// detectable: take the number of tokens the reference kept, compute the true cumulative
/// mass up to that point, and see whether it sits within a couple of ulp of the threshold.
///
/// Returns `None` when the boundary is not ambiguous -- which is the case for all but a
/// handful of the corpus -- so the caller fails as normal.
fn boundary_case(settings: &Value, logits: &[f32], expect: &[Value]) -> Option<String> {
    let p = settings.get("top_p").and_then(|v| v.as_f64())?;
    let kept = expect.iter().filter(|v| !matches!(v, Value::Null)).count();
    let masked = logits.len().checked_sub(kept)?;
    if masked == 0 || masked >= logits.len() {
        return None;
    }
    // The *implementation's* threshold, not a recomputed one: see `sample::top_p_threshold`.
    let threshold = sample::top_p_threshold(p) as f64;
    let probs = sample::softmax(logits);
    let total: f64 = probs.iter().map(|&x| x as f64).sum();
    if total <= 0.0 || total.is_nan() {
        return None;
    }
    // The ascending scan removes the *smallest* tokens, so the mass the rule compares is
    // that of the `masked` smallest -- not of the ones that survive.
    // Sorted the way the filter sorts: by value, with the same tie order, so the prefix of
    // `masked` indices is the set the ascending scan removed.
    let mut idx: Vec<usize> = (0..logits.len()).collect();
    idx.sort_by(|&a, &b| {
        logits[a].partial_cmp(&logits[b]).unwrap_or(std::cmp::Ordering::Equal)
    });
    let mass: f64 = idx[..masked].iter().map(|&i| probs[i] as f64 / total).sum();
    let ulp = f64::from_bits(threshold.to_bits().saturating_add(1)) - threshold;
    let gap = (mass - threshold).abs();
    if gap <= 2.0 * ulp {
        Some(format!(
            "masks {masked} of {}, cumulative mass {mass:.17} vs threshold {threshold:.17} \
             ({} ulp, gap {gap:.2e})",
            logits.len(),
            (gap / ulp).round() as i64
        ))
    } else {
        None
    }
}

fn last_stage(settings: &Value) -> String {
    for k in [
        "typical_p",
        "min_p",
        "top_p",
        "top_k",
        "temperature",
        "no_repeat_ngram_size",
        "frequency_penalty",
        "presence_penalty",
        "repetition_penalty",
    ] {
        if settings.get(k).is_some() {
            return k.to_string();
        }
    }
    "none".to_string()
}

fn config_from(settings: &Value) -> SamplerConfig {
    // `f64`, straight from the JSON: the reference stores these as Python floats and the
    // rounding to `f32` changes the truncation threshold, so the value has to survive
    // intact. See `sample::top_p`.
    let num = |k: &str, d: f64| settings.get(k).and_then(|v| v.as_f64()).unwrap_or(d);
    SamplerConfig {
        temperature: num("temperature", 1.0),
        top_k: settings.get("top_k").and_then(|v| v.as_usize()).unwrap_or(0),
        top_p: num("top_p", 1.0),
        min_p: num("min_p", 0.0),
        typical_p: num("typical_p", 1.0),
        repetition_penalty: num("repetition_penalty", 1.0),
        presence_penalty: num("presence_penalty", 0.0),
        frequency_penalty: num("frequency_penalty", 0.0),
        no_repeat_ngram_size: settings
            .get("no_repeat_ngram_size")
            .and_then(|v| v.as_usize())
            .unwrap_or(0),
        seed: 1,
    }
}

/// Draw many tokens from a few distributions and compare the empirical frequencies to the
/// softmax.
///
/// This is the only honest test of a sampler. A wrong inverse-CDF, or an RNG missing a mix
/// step, produces a plausible token on every single draw; the error only appears in the
/// proportions. Five sigma is the bound, with an absolute floor so that a probability of
/// `1e-9` is not required to be measured more precisely than the sample size allows.
fn frequency_check(draws: usize, verbose: bool) -> bool {
    let cases: Vec<(&str, Vec<f32>, SamplerConfig)> = vec![
        (
            "six tokens, one dominant",
            vec![3.0, 1.0, 0.5, 0.1, -1.0, -5.0],
            SamplerConfig { temperature: 1.0, ..Default::default() },
        ),
        (
            "64 tokens, nearly flat (the tail must be reachable)",
            (0..64).map(|i| -(i as f32) * 0.09).collect(),
            SamplerConfig { temperature: 1.0, ..Default::default() },
        ),
        (
            "two masked tokens (they must never be drawn)",
            {
                let mut v = vec![0.0f32; 32];
                v[3] = FILTERED;
                v[17] = FILTERED;
                v
            },
            SamplerConfig { temperature: 1.0, ..Default::default() },
        ),
        (
            "with top_k=2, so the filtered distribution is the one checked",
            vec![1.0f32, 1.0, 1.0, 1.0],
            SamplerConfig { temperature: 1.0, top_k: 2, ..Default::default() },
        ),
        (
            "temperature 0.5 spreads the tail out",
            vec![2.0f32, 1.5, 1.0, 0.5, 0.0, -0.5, -1.0],
            SamplerConfig { temperature: 0.5, ..Default::default() },
        ),
    ];

    let mut all_ok = true;
    for (ci, (label, logits, cfg)) in cases.into_iter().enumerate() {
        let mut s = match sample::Sampler::new(SamplerConfig { seed: 0xC0FFEE + ci as u64, ..cfg }) {
            Ok(s) => s,
            Err(e) => {
                println!("  !! case {ci}: {e}");
                all_ok = false;
                continue;
            }
        };
        // The target is the filtered softmax, computed the way the sampler computes it.
        let mut filtered = logits.clone();
        s.apply_filters(&mut filtered, &[]);
        let target = sample::softmax(&filtered);
        let total: f64 = target.iter().map(|&x| x as f64).sum();

        let mut counts = vec![0usize; logits.len()];
        let mut impossible = 0usize;
        let mut error = None;
        for _ in 0..draws {
            let mut l = logits.clone();
            match s.next(&mut l, &[]) {
                Ok(t) => {
                    counts[t as usize] += 1;
                    if target[t as usize] == 0.0 {
                        impossible += 1;
                    }
                }
                Err(e) => {
                    error = Some(e);
                    break;
                }
            }
        }
        if let Some(e) = error {
            println!("  !! case {ci} ({label}): {e}");
            all_ok = false;
            continue;
        }

        let mut worst_z = 0.0f64;
        let mut worst_dev = 0.0f64;
        let mut worst_tok = 0usize;
        let mut zero_mass_drawn = 0usize;
        for (i, &p) in target.iter().enumerate() {
            let t = p as f64 / total;
            if t == 0.0 {
                if counts[i] > 0 {
                    zero_mass_drawn += 1;
                }
                continue;
            }
            let got = counts[i] as f64 / draws as f64;
            let sigma = (t * (1.0 - t) / draws as f64).sqrt();
            let dev = (got - t).abs();
            let z = if sigma > 0.0 { dev / sigma } else { 0.0 };
            if z > worst_z {
                worst_z = z;
                worst_dev = dev;
                worst_tok = i;
            }
        }
        let reachable = target.iter().filter(|&&p| p > 0.0).count();
        let drawn = counts.iter().filter(|&&c| c > 0).count();
        let ok = zero_mass_drawn == 0 && impossible == 0 && (worst_z < 5.0 || worst_dev < 1e-4);
        if !ok {
            all_ok = false;
        }
        println!(
            "     {label}\n       {} target(s), {drawn} drawn, worst token {worst_tok} off by \
             {worst_dev:.2e} ({worst_z:.2} sigma){}",
            reachable,
            if impossible > 0 {
                format!("  !! {impossible} draws of a masked token")
            } else if zero_mass_drawn > 0 {
                format!("  !! {zero_mass_drawn} zero-mass tokens drawn")
            } else {
                String::new()
            }
        );
        if verbose {
            println!(
                "       target {:.6}  got {:.6}",
                target[worst_tok],
                counts[worst_tok] as f64 / draws as f64
            );
        }
    }
    if all_ok {
        println!("   every draw frequency is within 5 sigma of its probability");
    }
    all_ok
}
