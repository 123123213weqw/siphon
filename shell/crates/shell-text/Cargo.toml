[package]
name = "shell-text"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Model-agnostic text utilities: Python-compatible JSON, Unicode general categories, and NFC."

[dependencies]
